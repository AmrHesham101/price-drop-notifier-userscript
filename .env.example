# MongoDB Atlas connection string (replace <db_username> and <db_password> with your credentials)
MONGODB_URI=mongodb+srv://<db_username>:<db_password>@cluster0.k2co0g2.mongodb.net/price-drop-notifier?retryWrites=true&w=majority&appName=Cluster0

# For local MongoDB, use:
# MONGODB_URI=mongodb://localhost:27017/price-drop-notifier

# Server port (optional, defaults to 3000)
PORT=3000

# For production: use real SMTP credentials instead of Ethereal test accounts
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
